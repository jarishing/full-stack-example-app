{
  "name": "@conduit/database",
  "version": "1.0.0",
  "description": "Database schemas, migrations, and ORM configuration",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "db:migrate": "drizzle-kit migrate",
    "db:generate": "drizzle-kit generate:pg",
    "db:studio": "drizzle-kit studio",
    "lint": "echo 'Database package: No source files to lint yet'",
    "lint:fix": "echo 'Database package: No source files to lint yet'"
  },
  "dependencies": {
    "@conduit/utils": "*",
    "drizzle-orm": "^0.29.1",
    "pg": "^8.11.3",
    "drizzle-kit": "^0.20.7"
  },
  "devDependencies": {
    "@types/pg": "^8.10.9",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
} 